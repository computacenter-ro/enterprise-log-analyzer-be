# =============================================================================
# Telegraf Environment Variables
# =============================================================================
#
# Copy this file to telegraf.env and fill in the values.
# Then source it before running Telegraf:
#   source telegraf.env && telegraf --config telegraf.conf
#
# Or export variables in your shell profile (~/.zshrc or ~/.bashrc)
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Authentication (from /api/v1/sources response)
# -----------------------------------------------------------------------------
# Token for authenticating with the backend API
# Obtained when creating a Telegraf data source via POST /api/v1/sources
TELEGRAF_TOKEN=your-token-here

# Agent ID for identifying this Telegraf instance
# Obtained when creating a Telegraf data source via POST /api/v1/sources
TELEGRAF_AGENT_ID=your-agent-id-here

# -----------------------------------------------------------------------------
# REQUIRED: Backend Connection
# -----------------------------------------------------------------------------
# URL of the Enterprise Log Analyzer backend API
# For local development:
BACKEND_URL=http://localhost:8000

# For production (replace with your actual URL):
# BACKEND_URL=https://api.your-domain.com

# -----------------------------------------------------------------------------
# OPTIONAL: Agent Identification
# -----------------------------------------------------------------------------
# Override the hostname (default: system hostname)
# HOSTNAME=my-mac-server

# Environment identifier for tagging
ENVIRONMENT=development

# Region or datacenter identifier
REGION=local

# Service group for grouping related hosts
SERVICE_GROUP=enterprise-log-analyzer

# -----------------------------------------------------------------------------
# OPTIONAL: Collection Settings
# -----------------------------------------------------------------------------
# How often to collect data (default: 10s)
COLLECTION_INTERVAL=10s

# How often to flush data to outputs (default: 10s)
FLUSH_INTERVAL=10s

# -----------------------------------------------------------------------------
# OPTIONAL: Script Paths
# -----------------------------------------------------------------------------
# Path to the directory containing Telegraf scripts
# Apple Silicon Mac (Homebrew):
TELEGRAF_SCRIPTS_PATH=/opt/homebrew/etc/telegraf/scripts

# Intel Mac (Homebrew):
# TELEGRAF_SCRIPTS_PATH=/usr/local/etc/telegraf/scripts

# Custom location:
# TELEGRAF_SCRIPTS_PATH=/path/to/your/scripts

# -----------------------------------------------------------------------------
# OPTIONAL: InfluxDB Output (Uncomment when ready)
# -----------------------------------------------------------------------------
# URL of the InfluxDB server
# INFLUXDB_URL=http://localhost:8086

# InfluxDB authentication token
# INFLUXDB_TOKEN=your-influxdb-token

# InfluxDB organization name
# INFLUXDB_ORG=aiops

# InfluxDB bucket for storing metrics
# INFLUXDB_BUCKET=telemetry
